import React, { FC, memo } from 'react';
import AppBarComponent from '../ui/AppBar';

const Header: FC = () => {
	return (
		<>
			<AppBarComponent />
		</>
	);
};
export default memo(Header);
